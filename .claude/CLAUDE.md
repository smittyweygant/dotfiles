# Standing context — all projects

## Obsidian vault (second brain)

Canonical note store: `/Users/smittyweygant/Library/Mobile Documents/iCloud~md~obsidian/Documents/Smitty's Vault/`

When asked to jot down a note, capture a thought, or set a reminder — regardless of what project you're currently working in — write it there rather than losing it in the conversation:

- **Reminders / to-dos**: append a checkbox line under `## Tasks` in today's daily note, `Smitty's Vault/YYYY-MM-DD.md` (create the file from `Smitty's Vault/Templates/Daily Note Template.md` if it doesn't exist yet for today).
- **Everything else** (ideas, things to look into, loose notes): append to `Smitty's Vault/Inbox.md`.

Both of those are pre-approved for autonomous writes (see the vault's own `CLAUDE.md` for the full write-autonomy policy) — no need to ask before capturing. Don't edit any *other* file in the vault from outside a session actually working in the vault; if something needs to go on an existing note rather than the inbox/daily note, just flag it to the user instead of writing it yourself.

For anything beyond quick capture — processing the inbox, editing existing notes, vault structure questions — that's `Smitty's Vault/CLAUDE.md` and the `vault-triage` skill, both scoped to sessions actually working in that directory.
